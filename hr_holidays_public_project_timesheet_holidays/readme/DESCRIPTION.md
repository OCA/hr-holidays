When the module `project_timesheet_holidays` is installed, Odoo will generate timesheet
entries for leaves, and also for public holidays (if they are managed in the standard Odoo way).
This module allows to get the same behavior for public holidays managed with `hr_holidays_public`.
