On the wizard generation for multiple allocations, enforce that the date for allocations cannot be lower than the contract date.

This structure has a lot of sense for acumullation plans, where it has no sense to acumullate before your contract date.
