This module manages automatically accrual allocations by employee tags.
When a new employee is created, accrual allocations are created starting
from active accrual plans, based on its tags. Adding tags later and
removing them is also managed.
